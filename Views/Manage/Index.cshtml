@model IndexViewModel
@{
    ViewData["Title"] = "Gerez votre compte";
}

<h2>@ViewData["Title"].</h2>
<p class="text-success">@ViewData["StatusMessage"]</p>

<div>
    <h>Modifiez les parametres de votre compte</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>Mot de passe :</dt>
        <dd>
            @if (Model.HasPassword)
            {
                <a asp-controller="Manage" asp-action="ChangePassword" class="btn-bracketed">Change</a>
            }
            else
            {
                <a asp-controller="Manage" asp-action="SetPassword" class="btn-bracketed">Create</a>
            }
        </dd>
        <dt>Autre identifiant :</dt>
        <dd>

            @Model.Logins.Count <a asp-controller="Manage" asp-action="ManageLogins" class="btn-bracketed">Modifier</a>
        </dd>
        <dt>Numero de telephone :</dt>
        <dd>
            <p>
                Le numero de telephone peut etre utilise pour un niveau d'identification en 2 etapes, encore plus securise.
                Lire <a href="https://go.microsoft.com/fwlink/?LinkID=532713">cet article</a>
                pour plus de details sur comment creer cette application ASP.NET application afin de pouvoir utiliser l'authentification en 2 etapes par SMS.
            </p>
            @*@(Model.PhoneNumber ?? "None")
                @if (Model.PhoneNumber != null)
                {
                    <br />
                    <a asp-controller="Manage" asp-action="AddPhoneNumber" class="btn-bracketed">Change</a>
                    <form asp-controller="Manage" asp-action="RemovePhoneNumber" method="post">
                        [<button type="submit" class="btn-link">Remove</button>]
                    </form>
                }
                else
                {
                    <a asp-controller="Manage" asp-action="AddPhoneNumber" class="btn-bracketed">Add</a>
                }*@
        </dd>

        <dt>La verification en 2 etapes :</dt>
        <dd>
            <p>
                Aucun service d'authentification en 2 etapes n'a ete configure. Lire <a href="https://go.microsoft.com/fwlink/?LinkID=532713">cet article</a>
                afin de pouvoir utiliser la verification en 2 etapes sur cette application.
            </p>
            @*@if (Model.TwoFactor)
                {
                    <form asp-controller="Manage" asp-action="DisableTwoFactorAuthentication" method="post" class="form-horizontal">
                        Enabled <button type="submit" class="btn-link btn-bracketed">Disable</button>
                    </form>
                }
                else
                {
                    <form asp-controller="Manage" asp-action="EnableTwoFactorAuthentication" method="post" class="form-horizontal">
                        <button type="submit" class="btn-link btn-bracketed">Enable</button> Disabled
                    </form>
                }*@
        </dd>
    </dl>
</div>
